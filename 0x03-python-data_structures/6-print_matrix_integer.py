#!/usr/bin/python3
def print_matrix_integer(matrix=[[]]):
    matrix = [[column * row for column in range(len(matrix))] for 
            row in range(len(matrix[column))]
    print("{:d}".formt(matrix[column][row]), end="")
    if(column != (len(matrix[i] - 1):
        print(" ", end="")
